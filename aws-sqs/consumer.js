(async () => {
    require('dotenv').config();
    const sqsClient = require('./sqsClient');

    try {
        //work in progress
    } catch (error) {
        console.log(`ERRO: ${error}`);
    }
})();